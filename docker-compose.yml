version: "3.9"

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"
    container_name: password_in_log_php
    working_dir: /var/www/html
    command: php -S 0.0.0.0:8000 -t public
